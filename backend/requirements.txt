# MediSafe Clinic Ver-1 - 백엔드 의존성
# 개인정보보호법 제29조 준수 보안 라이브러리 포함

# ── 웹 프레임워크 ──
fastapi==0.115.0
uvicorn[standard]==0.30.6

# ── 데이터베이스 ──
sqlalchemy==2.0.35
psycopg2-binary==2.9.9    # PostgreSQL 드라이버
alembic==1.13.3            # DB 마이그레이션

# ── 인증/보안 ──
python-jose[cryptography]==3.3.0  # JWT
argon2-cffi==23.1.0               # Argon2id 비밀번호 해시 (NIST SP 800-63B)
cryptography==43.0.1              # 암호화 기반 라이브러리

# ── API 보안 ──
slowapi==0.1.9              # Rate Limiting
python-multipart==0.0.12   # Form 데이터

# ── 입력 검증 ──
pydantic==2.9.2
pydantic-settings==2.5.2
pydantic[email]             # 이메일 형식 검증

# ── 캐시/세션 ──
redis==5.1.1

# ── PDF 보고서 ──
reportlab==4.2.5

# ── 이메일 알림 ──
aiosmtplib==3.0.1

# ── 유틸 ──
python-dotenv==1.0.1
python-dateutil==2.9.0
